@model IEnumerable<DataModelsFramework.Song>

@{
    ViewBag.Title = "index";
}

<h2>index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SongName)
        </th>
        <th></th>
    </tr>

@foreach (var song in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => song.SongName)
        </td>
        @*<td>
            @foreach (var )
            @Html.DisplayFor(modelItem => song)
        </td>*@
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=song.SongId }) |
            @Html.ActionLink("Details", "Details", new { id=song.SongId }) |
            @Html.ActionLink("Delete", "Delete", new { id=song.SongId })
        </td>
    </tr>
}

</table>
