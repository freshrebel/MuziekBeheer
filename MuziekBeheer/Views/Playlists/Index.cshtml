@model IEnumerable<DataModelsFramework.Playlist>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Maak nieuw", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayName("Naam")
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PlaylistName)
        </td>
        <td>
            @Html.ActionLink("Bewerk", "Edit", new { id=item.PlaylistId }) |
            @Html.ActionLink("Details", "Details", new { id=item.PlaylistId }) |
            @Html.ActionLink("Verwijder", "Delete", new { id=item.PlaylistId })
        </td>
    </tr>
}

</table>
