@model IEnumerable<DataModelsFramework.Artist>

@{
    ViewBag.Title = "Index";
}

<h2>Artiesten</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayName("Naam")
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ArtistName)
        </td>
        <td>
            @Html.ActionLink("Bewerk", "Edit", new { id=item.ArtistId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ArtistId }) |
            @Html.ActionLink("Verwijder", "Delete", new { id=item.ArtistId })
        </td>
    </tr>
}

</table>
